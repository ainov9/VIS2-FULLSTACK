╔══════════════════════════════════════════════════════════════════════════╗
║          STUDENT VALIDATION SYSTEM - NODE.JS + EXPRESS                  ║
║                     INSTALLATION GUIDE                                   ║
║                        Version 1.0                                       ║
╚══════════════════════════════════════════════════════════════════════════╝

📋 QUICK START GUIDE (5 Minutes Setup)
═══════════════════════════════════════════════════════════════════════════

STEP 1: PREREQUISITES
─────────────────────
✓ Node.js 16+ installed: https://nodejs.org/
✓ MySQL 5.7+ running
✓ Code editor (VS Code recommended)

Check your versions:
  node --version
  npm --version
  mysql --version

STEP 2: EXTRACT FILES
─────────────────────
Extract the ZIP file to any location:
  Windows: C:\Projects\student-system-nodejs\
  Mac/Linux: ~/Projects/student-system-nodejs/

STEP 3: INSTALL DEPENDENCIES
────────────────────────────
Open terminal in project folder and run:

  npm install

This installs all required packages:
✓ express (web framework)
✓ mysql2 (database driver)
✓ ejs (template engine)
✓ multer (file uploads)
✓ dotenv (configuration)
✓ And more...

Wait for installation to complete (~30 seconds)

STEP 4: CREATE DATABASE
───────────────────────
Option A - Using phpMyAdmin:
1. Open: http://localhost/phpmyadmin
2. Click "New"
3. Database name: student_validation_system
4. Click "Import"
5. Choose: database.sql
6. Click "Go"

Option B - Using MySQL Command Line:
  mysql -u root -p
  CREATE DATABASE student_validation_system;
  USE student_validation_system;
  SOURCE /path/to/database.sql;
  EXIT;

STEP 5: CONFIGURE
────────────────
Open the .env file and verify settings:

  PORT=3000
  DB_HOST=localhost
  DB_USER=root
  DB_PASSWORD=           ← Add your MySQL password if any
  DB_NAME=student_validation_system
  DB_PORT=3306

Save the file if you made changes.

STEP 6: START SERVER
───────────────────
Run the application:

  npm start

You should see:
╔═══════════════════════════════════════════════════════════╗
║   🎓 Student Validation System - Node.js                 ║
╟───────────────────────────────────────────────────────────╢
║   🚀 Server running on: http://localhost:3000            ║
║   📊 Database: student_validation_system                 ║
║   🌍 Environment: development                            ║
╟───────────────────────────────────────────────────────────╢
║   Press Ctrl+C to stop                                   ║
╚═══════════════════════════════════════════════════════════╝

STEP 7: ACCESS APPLICATION
──────────────────────────
Open your browser:

  http://localhost:3000

You should see the Student Validation System! 🎉

═══════════════════════════════════════════════════════════════════════════

🎯 TESTING THE APPLICATION
═══════════════════════════════════════════════════════════════════════════

Test 1: View Sample Students
→ Click "All Students"
→ You should see 3 pre-loaded students

Test 2: Register New Student
→ Click "Register Student"
→ Fill the form
→ Submit
→ Check if Student ID was generated

Test 3: Start Validation Session
→ Go to home page
→ Enter session name
→ Enter 3 students with grades
→ See statistics and chart

Test 4: Save Session
→ Click "Save Session to Database"
→ Go to "History"
→ See your saved session

═══════════════════════════════════════════════════════════════════════════

🔧 DEVELOPMENT MODE
═══════════════════════════════════════════════════════════════════════════

For development with auto-reload:

  npm run dev

This uses nodemon to automatically restart when you edit files.

═══════════════════════════════════════════════════════════════════════════

⚠️ TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════════

PROBLEM: "Cannot find module 'express'"
SOLUTION:
→ Run: npm install
→ Make sure you're in the project folder

PROBLEM: "Port 3000 already in use"
SOLUTION:
→ Option 1: Change port in .env file (PORT=3001)
→ Option 2: Kill process using port 3000
  Windows: netstat -ano | findstr :3000
           taskkill /PID <PID> /F
  Mac/Linux: lsof -i :3000
             kill -9 <PID>

PROBLEM: "Database connection failed"
SOLUTION:
→ Check MySQL is running
→ Verify .env credentials
→ Test: mysql -u root -p
→ Make sure database exists

PROBLEM: "Cannot POST /api/students"
SOLUTION:
→ Server not running - check terminal
→ Restart: npm start
→ Check for error messages

PROBLEM: "File upload not working"
SOLUTION:
→ Check folder exists: public/uploads/photos/
→ Create if missing: mkdir -p public/uploads/photos
→ Check permissions (Linux/Mac): chmod 755 public/uploads

PROBLEM: "Blank page or errors"
SOLUTION:
→ Check browser console (F12)
→ Check server terminal for errors
→ Try: rm -rf node_modules && npm install

═══════════════════════════════════════════════════════════════════════════

📁 FILE STRUCTURE
═══════════════════════════════════════════════════════════════════════════

student-system-nodejs/
├── server.js              Main server file
├── package.json           Dependencies
├── .env                   Configuration
├── database.sql           Database schema
├── README.md              Documentation
│
├── config/
│   └── database.js        DB connection
│
├── routes/
│   ├── students.js        Student API
│   ├── validation.js      Validation API
│   └── sessions.js        Sessions API
│
├── views/
│   ├── index.ejs          Main page
│   ├── inscription.ejs    Registration
│   ├── students.ejs       Students list
│   └── history.ejs        History
│
└── public/
    ├── css/               Stylesheets
    ├── js/                JavaScript
    └── uploads/           Photos

═══════════════════════════════════════════════════════════════════════════

🌐 API ENDPOINTS
═══════════════════════════════════════════════════════════════════════════

Students:
  GET    /api/students           Get all students
  GET    /api/students/:id       Get one student
  POST   /api/students           Register student
  PUT    /api/students/:id       Update student
  DELETE /api/students/:id       Delete student

Validation:
  POST   /api/validation/save-session    Save session
  POST   /api/validation/add-grade       Add grade
  GET    /api/validation/statistics      Get stats

Sessions:
  GET    /api/sessions              Get all sessions
  GET    /api/sessions/:id          Get session details
  DELETE /api/sessions/:id          Delete session

═══════════════════════════════════════════════════════════════════════════

💡 USEFUL COMMANDS
═══════════════════════════════════════════════════════════════════════════

# Start server
npm start

# Development with auto-reload
npm run dev

# Install dependencies
npm install

# Install specific package
npm install package-name

# Update packages
npm update

# Check for outdated packages
npm outdated

# Clear npm cache (if issues)
npm cache clean --force

═══════════════════════════════════════════════════════════════════════════

🚀 PRODUCTION DEPLOYMENT
═══════════════════════════════════════════════════════════════════════════

1. Set NODE_ENV=production in .env
2. Use process manager (PM2):
   npm install -g pm2
   pm2 start server.js
   pm2 startup
   pm2 save

3. Use reverse proxy (nginx):
   location / {
     proxy_pass http://localhost:3000;
   }

4. Enable HTTPS
5. Set strong SESSION_SECRET

═══════════════════════════════════════════════════════════════════════════

🎓 DIFFERENCES FROM PHP VERSION
═══════════════════════════════════════════════════════════════════════════

Advantages of Node.js version:
✅ Faster performance (async I/O)
✅ Modern JavaScript (ES6+)
✅ RESTful API structure
✅ Better for real-time features
✅ npm ecosystem
✅ Easy deployment
✅ WebSocket ready

Same features:
✓ Student ID generation
✓ Database structure
✓ All CRUD operations
✓ Validation system
✓ Export capabilities
✓ Same UI/UX

═══════════════════════════════════════════════════════════════════════════

📚 LEARNING RESOURCES
═══════════════════════════════════════════════════════════════════════════

Node.js: https://nodejs.org/docs/
Express: https://expressjs.com/
MySQL2: https://github.com/sidorares/node-mysql2
EJS: https://ejs.co/
Multer: https://github.com/expressjs/multer

═══════════════════════════════════════════════════════════════════════════

✅ POST-INSTALLATION CHECKLIST
═══════════════════════════════════════════════════════════════════════════

Before using:
□ Node.js installed
□ npm install completed
□ MySQL running
□ Database created
□ database.sql imported
□ .env configured
□ npm start works
□ http://localhost:3000 accessible
□ Can register student
□ Can start validation
□ Charts display
□ Can save session

═══════════════════════════════════════════════════════════════════════════

🎉 YOU'RE ALL SET!
═══════════════════════════════════════════════════════════════════════════

Your Node.js Student Validation System is ready!

Visit: http://localhost:3000

Need help? Check README.md for detailed documentation.

Happy teaching! 🎓

═══════════════════════════════════════════════════════════════════════════
Built with ❤️ using Node.js + Express + MySQL
Version 1.0 - February 2026
