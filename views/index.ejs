
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body>
    <div class="container">
        <header>
            <h1>🎓 Student Validation System</h1>
            <p>Professional Grade Management - Node.js Edition</p>
            <nav class="main-nav">
                <a href="/" class="nav-btn active">🏠 Validation</a>
                <a href="/inscription" class="nav-btn">➕ Register Student</a>
                <a href="/students" class="nav-btn">👥 All Students</a>
                <a href="/history" class="nav-btn">📊 History</a>
            </nav>
        </header>

        <div class="validation-section">
            <div class="input-section">
                <h2>Start Validation Session</h2>
                <div class="form-group">
                    <label for="sessionName">Session Name (Optional):</label>
                    <input type="text" id="sessionName" placeholder="e.g., Fall 2025 Final Exams">
                </div>
                <div class="form-group">
                    <label for="totalStudents">Number of students:</label>
                    <input type="number" id="totalStudents" value="20" min="1" max="100">
                    <button onclick="start()" class="btn btn-primary">Start Session</button>
                </div>
            </div>

            <div id="formArea" class="form-area"></div>

            <div class="results-section" id="resultsSection" style="display:none;">
                <div class="table-container">
                    <div class="table-header">
                        <h3>📋 Students Results</h3>
                        <div class="table-actions">
                            <input type="text" id="searchTable" placeholder="🔍 Search..." onkeyup="filterTable()">
                            <button onclick="sortTable('name')" class="btn-sm">Sort by Name</button>
                            <button onclick="sortTable('avg')" class="btn-sm">Sort by Average</button>
                        </div>
                    </div>
                    <table id="resultsTable">
                        <thead>
                            <tr>
                                <th>Student ID</th>
                                <th>Name</th>
                                <th>Average</th>
                                <th>Status</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody id="tableBody"></tbody>
                    </table>
                </div>

                <div class="statistics-chart-container">
                    <div class="chart-wrapper">
                        <canvas id="chart"></canvas>
                    </div>
                    
                    <div class="stats-box">
                        <h3>📊 Statistics</h3>
                        <div class="stat-item">
                            <span class="stat-label">Total Students:</span>
                            <span class="stat-value" id="totalCount">0</span>
                        </div>
                        <div class="stat-item">
                            <span class="stat-label">Validé:</span>
                            <span class="stat-value stat-success" id="validCount">0</span>
                        </div>
                        <div class="stat-item">
                            <span class="stat-label">Rattrapage:</span>
                            <span class="stat-value stat-warning" id="rattCount">0</span>
                        </div>
                        <div class="stat-item">
                            <span class="stat-label">Non Validé:</span>
                            <span class="stat-value stat-danger" id="nvCount">0</span>
                        </div>
                        <div class="stat-item">
                            <span class="stat-label">Success Rate:</span>
                            <span class="stat-value" id="successRate">0%</span>
                        </div>
                    </div>
                </div>

                <div class="action-buttons">
                    <button onclick="saveSessionToDB()" class="btn btn-success">💾 Save Session to Database</button>
                    <button onclick="exportToExcel()" class="btn btn-info">📊 Export to Excel</button>
                    <button onclick="exportToJSON()" class="btn btn-secondary">📄 Download JSON</button>
                    <button onclick="printResults()" class="btn btn-secondary">🖨️ Print Results</button>
                    <button onclick="resetSession()" class="btn btn-danger">🔄 Reset Session</button>
                </div>
            </div>
        </div>
    </div>

    <script src="/js/app.js"></script>
</body>
</html>
